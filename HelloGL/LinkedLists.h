#pragma once
#include "Structures.h"
#include "SceneObject.h"
#include <iostream>
#include <iomanip>
using namespace std;

class LinkedLists //I began doing linked lsists and made it funtional but didnt implement it into the project
{
private:
	ListNode* head = nullptr;
public:
	LinkedLists();
	~LinkedLists();

	//ListNode* MakeNode(ListNode** head, SceneObject** object);
	//void PrintList(ListNode* node);
};


